import { RenderGroup } from "./type/RenderGroup";

export const reorderGroup = (group: RenderGroup) => {
  // group?.forEach((state, index) => {
  //   state.actions.setZ(index);
  //   if (index === group.length - 1) {
  //     state.actions.setPointer("auto");
  //     return;
  //   }

  //   state.actions.setPointer("none");
  // });
};
